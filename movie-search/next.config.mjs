/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['static.tvmaze.com'], // add the API's image domain here
  },
}


export default nextConfig;
